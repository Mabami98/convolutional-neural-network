#ifndef PERFORMANCE_SUMMARY_HPP
#define PERFORMANCE_SUMMARY_HPP

#include <vector>

using namespace std;

void print_performance_summary(const vector<int>& predictions,
                               const vector<int>& ground_truth);

#endif
